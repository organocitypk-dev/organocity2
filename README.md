# OrganoCity

Full-stack e-commerce storefront built with Next.js, Prisma, NextAuth, and Cloudinary.

## Meta Pixel deployment

Set `NEXT_PUBLIC_META_PIXEL_ID` to the OrganoCity Pixel ID in every deployment environment that should send browser events, then redeploy. Leave it unset where tracking should remain disabled.
